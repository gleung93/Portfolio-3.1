<div class="hero" style="background-image: url('{{ img-src }}')">
	{{#if darken}}
	<div class="darken"></div>
	{{/if}}
	<div class="container vertical-align">
		<div class="row">
			<div class="col-md-6 text-white">
				<h1>{{ page-title }}</h1>
				<h3>{{ sub-title1 }}</h3>
			</div>
		</div>
	</div>
</div>
