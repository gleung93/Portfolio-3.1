<section class="{{ class }}">
	<div class="container">
		<div class="row">
			{{#each item}}
			<div class="col-md-4">
				<img src="{{ img-src }}" class="center-block">
				<h3>{{ title }}</h3>
				<p>{{ description }}</p>
			</div>
			{{/each}}
		</div>
	</div>
</section>